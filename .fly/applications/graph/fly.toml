app = 'minha-receita-graph'
primary_region = 'ams'

[build]
  image = 'atcr.io/cuducos.me/minha-receita:latest'

[experimental]
  cmd = ["graph", "--skip-create"]

[env]
  PORT = '8888'

[http_service]
  internal_port = 8888
  force_https = true
  auto_stop_machines = 'suspend'
  auto_start_machines = true
  min_machines_running = 0
  max_machines_running = 1
  processes = ['app']

[[http_service.checks]]
  grace_period = "30s"
  interval = "1m"
  method = "GET"
  timeout = "3s"
  path = "/healthz"

[[vm]]
  size = 'shared-cpu-1x'
